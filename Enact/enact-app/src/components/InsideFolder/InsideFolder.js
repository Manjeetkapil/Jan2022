import kind from "@enact/core/kind";

const InsideFolder = kind({
  name: "insidefolder",


  render: (props) => (
    <div>
        Hi finally i am insidefolder
    </div>
  ),
});

export default InsideFolder;